<%= form_for(@visit_type) do |f| %>
  <% if @visit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit_type.errors.count, "error") %> prohibited this visit_type from being saved:</h2>

      <ul>
      <% @visit_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
