<?xml version="1.0" encoding="utf-8"?>
<mx:TitleWindow xmlns:mx="http://www.adobe.com/2006/mxml" 
	layout="vertical" 
	width="400" 
	height="450"
	title="Add New Visit"
	showCloseButton="true"
    close="PopUpManager.removePopUp(this)">
	
	<mx:Script>
		<![CDATA[
			import mx.collections.ArrayCollection;
			import mx.managers.PopUpManager;
			import mx.rpc.events.ResultEvent;
			import com.matlabulchifai.events.patient.VisitEvent;
			
			[Bindable]
			public var patient:XML;
			[Bindable]
			public var patientID:Number;
			[Bindable]
			private var reasons:ArrayCollection = new ArrayCollection(
			[ { label:"Malaria", data:"malaria" }, { label:"Flu", data:"flu" }, 
				{ label:"Food poisoning", data:"food poisoning" }, { label:"Other...", data:"" }]);
			[Bindable]
			private var yesNo:ArrayCollection = new ArrayCollection(
			[ { label:"Yes", data:"yes" }, { label:"No", data:"no" } ]);
			
			private function processVisitCreate():void{
				svcVisitCreate.send();
				PopUpManager.removePopUp(this);
			}
			
			private function handleNewVisitResult(event:ResultEvent):void {
				var resultXML:XML = XML(event.result);
				Matlabulchifai.debug("visit created = \n" + resultXML);
				dispatchEvent(new VisitEvent(VisitEvent.VISIT_CREATE, resultXML));
			}
			[Bindable]
			public var languageArray:Object;
		]]>
	</mx:Script>
	
	<mx:HTTPService
		id="svcVisitCreate"
		url="/visits.xml"
		resultFormat="e4x"
		contentType="application/xml"
		method="POST"
		result="handleNewVisitResult(event)">
		<mx:request>
			<visit>
				<reason>{reasonCB.text + otherTI.text}</reason>
				<followUp>{followupCB.text}</followUp>
				<dateSeen>{visitDateDF.text}</dateSeen>
				<notes>{visitNotesTA.text}</notes>
				<patient_id>{patientID}</patient_id>
			</visit>
		</mx:request>
	</mx:HTTPService>
	
	
	<mx:VBox verticalAlign="middle" horizontalAlign="center" width="100%" height="85%">
		<mx:Form>
			<mx:Label text="{patient['last_name']}, {patient['first_name']}" fontWeight="bold" />					
			<mx:Spacer height="5" />
			<mx:FormItem label="Reason">
			<mx:ComboBox id="reasonCB" dataProvider="{reasons}" />
			</mx:FormItem>
			<mx:Spacer height="5" />
			<mx:FormItem label="Other">
				<mx:TextInput id="otherTI" enabled="false" width="100%"/>
			</mx:FormItem>
			<mx:Spacer height="5" />
			<mx:FormItem label="Follow up?">
				<mx:ComboBox id="followupCB" dataProvider="{yesNo}" />
			</mx:FormItem>
			<mx:Spacer height="5" />
			
			<mx:FormItem label="Date">
				<mx:DateField id="visitDateDF" yearNavigationEnabled="true" minYear="1980" />
			</mx:FormItem>
			<mx:Spacer height="5" />
			<mx:FormItem label="Notes">
				<mx:TextArea id="visitNotesTA" width="200" height="150" />
			</mx:FormItem>
		</mx:Form>
	</mx:VBox>
	
	
	<mx:HBox horizontalAlign="center" verticalAlign="middle" width="100%" height="15%">
		<mx:Button label="OK" id="OKBtn" click="processVisitCreate()" />
		<mx:Button label="Cancel" id="CancelBtn" click="PopUpManager.removePopUp(this)" />
	</mx:HBox>
	
</mx:TitleWindow>
